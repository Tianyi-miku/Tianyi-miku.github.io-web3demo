{
  "scripts": {
    "compile": "yarn hardhat compile",
    "deploy_s": "yarn hardhat run scripts/deploy.ts --network sepolia",
    "deploy_m": "yarn hardhat run scripts/mockdeploy.ts --network localhost",
    "deploy_m_fill": "yarn hardhat run scripts/mock.ts --network localhost",
    "deploy_s_lottery": "yarn hardhat run scripts/lotterydeploy.ts --network sepolia",
    "dev": "yarn hardhat node",
    "test": "yarn hardhat test --network localhost",
    "test-staging": "yarn hardhat test --network sepolia",
    "lint": "yarn solhint 'contracts/*.sol'",
    "lint:fix": "yarn solhint 'contracts/**/*.sol' --fix",
    "coverage": "yarn hardhat coverage",
    "verity": "yarn hardhat verify --地址 （参数） --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0",
    "@typechain/ethers-v6": "^0.4.0",
    "@typechain/hardhat": "^8.0.0",
    "@types/chai": "^4.2.0",
    "@types/mocha": "^10.0.1",
    "chai": "^4.2.0",
    "hardhat": "^2.17.1",
    "hardhat-gas-reporter": "^1.0.8",
    "solidity-coverage": "^0.8.1",
    "ts-node": "^10.9.1",
    "typechain": "^8.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.6.1",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomiclabs/hardhat-ethers": "^2.2.3",
    "@openzeppelin/contracts": "^4.9.3",
    "dotenv": "^16.3.1",
    "hardhat-deploy": "^0.11.37"
  }
}